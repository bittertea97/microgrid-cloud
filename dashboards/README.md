# Dashboards

Grafana dashboards used by the dev/test compose stack.

- Place additional dashboards (JSON exports) in this folder.
- The default provisioning mounts this folder at `/var/lib/grafana/dashboards`.